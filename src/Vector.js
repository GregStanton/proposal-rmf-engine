/**
 * @module Vector
 */

/* Requirements:
 * array()
 * dist(v)
 * add(v), sub(v), mult(n) (or static)
 * copy()
 * equals(v)
 * mag()
*/

// just including this temporarily to make sure Vitest is set up properly
export function sum(a, b) {
  return a + b
}